name: ci

on:
  push:
    branches:
      [main]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Log in to YC
        uses: docker/login-action@v1
        with:
          registry: cr.yandex
          username: oauth
          password: AQAAAABZE_45AATuwcBCpt3xOU8nh66WX2gBseo
      -
         name: Build, tag, and push image to Yandex Cloud Container Registry
         run: |
          docker build -t cr.yandex/crpk1jor4l62cnmbcehk/time:latest .
          docker push cr.yandex/crpk1jor4l62cnmbcehk/time:latest
